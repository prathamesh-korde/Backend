<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <h2>Edit Your Post</h2>
    <p class="info">Post ID: <%= post.id %></p>
    <p class="info">Username: @<%= post.username %></p>

    <form method="post" action="/post/<%= post.id %>?_method=PATCH" class="form-container">
        <textarea rows="10" cols="50" name="content" class="textarea"><%= post.content %></textarea>
        <br>
        <button type="submit">Submit</button>
    </form>
</body>
</html>
